{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} no-margin">
            {{ message | raw }}
        </div>
    {% endfor %}
{% endfor %}
